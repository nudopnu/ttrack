# ttrack
A very simple time tracker that stores each tracker as a separate `csv` file under `~/ttrack`.

**Tip**
You can version control `~/ttrack` to sync between devices.

## Installation
```bash
pip install git+https://github.com/nudopnu/ttrack.git
```

## Usage
```bash
ttrack start test
ttrack stop test
ttrack summary test
```
